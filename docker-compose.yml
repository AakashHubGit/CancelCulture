version: '3.8'

services:
  cancelculture-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - COC_API_KEY=${COC_API_KEY}
    volumes:
      - ./data:/app/data
    restart: always
